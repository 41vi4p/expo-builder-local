# Environment / secrets
.env
.env.local

# Orchestrator
orchestrator/node_modules/
orchestrator/dist/
orchestrator/*.tsbuildinfo

# Persisted build data (SQLite db, logs, uploaded keystores) — never commit these
data/
*.db
*.db-journal
*.db-wal

# OS
.DS_Store
